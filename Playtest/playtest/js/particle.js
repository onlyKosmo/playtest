document.addEventListener('DOMContentLoaded', function() {
    // Créez un élément conteneur pour les particules
    var particlesContainer = document.createElement('div');
    particlesContainer.id = 'particles-js';
    document.body.appendChild(particlesContainer);  // Ajoute l'élément à la page

    // Le script pour initialiser Particles.js est déjà dans lib.js, donc aucune action supplémentaire ici
});